version: "3.4"
services:
  frontend:
    image: node:alpine
    container_name: frontend
    entrypoint: /bin/sh
    ports:
      - "5173:5173"
    working_dir: /srv/app
    volumes:
      - type: bind
        source: ./
        target: /srv/app
    tty: true
